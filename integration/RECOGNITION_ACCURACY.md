# 识别验证记录 · 1.9.7

## 1.9.7：显式深度复核与校正样本

此版增加人工第二轮检查工具，不替换原来的整图自动识别，也没有接入在线模型或训练模型。普通巴别语识别无改动；塞壬语的新增可选中心范围约束仅用于独立 `deepRegion` 分支，普通调用不受影响。

- 巴别语固定当前框，比对多种阈值及32/64细节尺度，排除已属于邻框的像素。候选支持次数仅表示这些方案的稳定性，不能当概率；同形不同ID保留平票。174个单字合成样本的首选仍为58/58、58/58、57/58，与原精细比对一致，**此基准没有测得准确率提升**。深度建议不直接写入识别字形或当前映射。
- 塞壬语固定当前基底，框仅限制候选中心，周围完整笔画仍参与核验；位置起点更密，保留多种合法尺寸/朝向及原图像素重建比较。仍保留±30°朝向、0.33–0.44尺寸比例及2.5%边界带。结果始终待复核，低于常规覆盖门槛的候选明确提示；点击预览后另行确认才采用。合成A、窄至亚像素的中心框、坐标平移、空白、圈外、输入不变性和真实Worker消息测试通过。开发参考中一个原先支持的E区域仍以E为首选；这不是新增独立准确率基准。
- 最新四组图按用户补充说明，第一组V朝向异常，排除该项考核；不为它放宽约束。1.9.7实际整图复测约29.62秒，仍读出 `SEEN / TYNIN / ESIXT / YFIVE`，19/19有效字母符合答案。原20字文本仍缺V，**不称为20/20**；这是参与开发的参考，不是盲测。
- 校正样本记录原图及已核实标注，可以修改、排除训练、删除及独立JSON导入导出。样本不自动成为模板或训练数据更新，不影响下一次识别；需要另行开展训练和保留独立测试集后，才能评价泛化提升。支持同图冲突的原子拒绝、保存期间新修改的脏状态保留、图片尺寸和格式校验。

隔离的无头Chrome测试通过：两行键盘在1400/390像素视口下保持13列对应、手机横滑、Siren真实Worker候选预览/确认、样本编辑与导出，以及巴别语深度候选与样本收录。无JS错误，无HTTP请求。仅测试临时浏览器，不操作用户已打开页面，不代表Android实机验证；独立与Assistant共用源代码和生成包。完整像素与存档回归见 `npm test`，浏览器复核可运行 `npm run test:deep-browser`。

以下保留旧版本记录；1.9.6所说“漏V”由此处用户确认的朝向异常说明补充。

## 1.9.6：高反差软边塞壬语（20字）

新参考为用户提供的QQ20260911-143055.png，原始1550×451，答案为 `SEVEN / TYNIN / ESIXT / YFIVE`。先运行1.9.5基线，再修改通用光度处理。整张图自动分组；没有向Worker提供答案、四个框、字形数量、坐标或读序。此图参与开发，改进后的结果不是独立盲测。

| 指标 | 1.9.5 | 1.9.6 |
| --- | --- | --- |
| 检出大螺旋 | 4/4 | 4/4 |
| 检出小螺旋 | 4/20 | 19/20 |
| 明确且与答案一致的字母 | 3 | 19 |
| 完整读序正确的组 | 0/4 | 3/4 |
| 字符编辑距离 | 17 | 1 |

| 组 | 用户答案 | 基线 | 改进后 |
| --- | --- | --- | --- |
| 1 | SEVEN | 空 | SEEN |
| 2 | TYNIN | Y | TYNIN |
| 3 | ESIXT | X | ESIXT |
| 4 | YFIVE | Y? | YFIVE |

**仍漏第一组V，并非20/20。** 本样本未观察到改进后的错误确定字母，但不能据此保证陌生截图准确率。实际Worker及网页相同读序模型约31.6秒；基线约17秒。原图反色后同样读出19项、后三组全对（约30.4秒）。候选增加后会多花拟合时间，速度并不代表手机性能。

### 改动依据与保留项

此图亮暗峰值差很大，但细线发灰，前景中间色阶占比约59.46%。以前只按峰值对比度分流，误把它当成清晰图。现在对高对比图统计归一化强度大于0.1的前景；其中强度小于0.9的像素超过一半时，使用现有Otsu软阈值。背景不参加分母。低对比照片沿用原流程，锐利高对比图仍用原线性处理。该统计是图像处理启发式，不是置信概率。

没有扩大字形尺寸、角度、外圈余量，也没有降低字母确认门槛或使用语义补字。尝试扩大尺寸、角度和局部位置重启均未稳定恢复V，且更慢，因此未合入。V在粗搜索有候选，精匹配的笔画像素支持不足；不能仅因已知答案就把它加入结果。

旧清晰四组、旧16组反色纹理图及BDAEFC裁片的归一化像素与1.9.5逐像素相同。实际整图回归：旧四组仍为 `FFT / YFOR / TYE / IGHT`，14/14有效字母；旧16组仍为11组完整正确、77个检出项、75个确定项、编辑距离8，原有错序/错字/漏项未宣称解决。锐利/软边分流、反色对称、背景排除及低对比路径有测试。巴别语算法未改动。

复现新样本（图片不打包、不上传；未指定路径明确跳过）：

```powershell
$env:SIREN_SOFT_FIXTURE='原始QQ20260911-143055.png的完整路径'
npm run test:siren-soft
```

离线页和Assistant接入包共用改进；未操作浏览器或手机。保存当前塞壬语工作后再刷新，重新识别原始图才能应用改进。

## 1.9.5：三张真实巴别语截图

使用用户提供的IMG_3501/3502/3503原始PNG（均1206×2622），先框出上方巴别语区；英文说明、字形键盘和其他界面不交给识别器。逐行视觉核验共206个字形ID，区分大小写及the、OF、a_word、and连字。标注仅存于测试，不写入识别算法，不读取用户映射来猜形。

三张原图原本已能给出全部正确首选，因此**本次不能宣称原图准确率从较低水平提升至100%**。实际提升是缩小后的窄行距识别，以及上下分离的独立字形。

| 原图 | 首选正确（前/后） | 自动确认（前/后） |
| --- | --- | --- |
| IMG_3501 | 59/59 → 59/59 | 59 → 59 |
| IMG_3502 | 42/42 → 42/42 | 42 → 42 |
| IMG_3503 | 105/105 → 105/105 | 103 → 103 |
| 合计 | 206/206 → 206/206 | 204 → 204 |

原图反色后得到相同结果。第三图两个x的整体形状与拆成A+a的像素形状十分接近，分割余量约0.0343、0.0238，低于0.045门槛，仍保留待复核。没有根据extracted/exceeding等词意提高置信度。

### 缩小压力测试（同一批图片，并非新的独立盲测）

按固定框裁出巴别语后，以双线性采样缩小。以下为严格字形ID的编辑距离，计入增项、漏项、替换；大小写和连字不同也算错。它是纠正整串所需的操作数，**不是百分比准确率**。

| 尺寸比例 | 图1 前→后 | 图2 前→后 | 图3 前→后 | 合计 前→后 |
| --- | --- | --- | --- | --- |
| 100% | 0→0 | 0→0 | 0→0 | 0→0 |
| 50% | 3→3 | 0→0 | 7→7 | 10→10 |
| 40% | 37→12 | 26→5 | 67→25 | 130→42 |
| 30% | 58→28 | 42→13 | 104→39 | 204→80 |

40%时行数从4/3/6恢复为正确的5/4/8；30%时从各1行恢复为5/4/8。40%与30%的字形高度不足30像素，仍全部待复核；恢复分行并没有解决所有字形混淆。原图单张本机约0.16–0.40秒，不包含文件解码与界面操作。截图裁片、采样相位及压缩不同可能改变结果。

### 实现和回归

- 保留一像素空行分隔，不再固定要求三像素空行；用完整模板证据重新组合上下分离的部件。合并要求全形评分至少0.90，并比每个碎片的最佳评分高至少0.06，同时限制间隙与外框比例；不指定某个字母或词语。
- 宽扁孤立碎片无可行模板时保留未知项，不因动态规划缺少兜底边而中断整行。不会丢掉碎片或填入英文。
- 58种独立字形在64/40像素合成图中从56/58改善到58/58，修复the/a_word被分行的问题；原生模板58/58通过。1–2像素密行、孤立连字和宽碎片均有回归。
- 原58字形多行合成基准仍为173/174，映射、分词、中文机翻及塞壬语算法没有改变。原图不上传，网页继续离线识别。

可复现真实图片测试（只读取用户明确指定目录下这三个文件，图片不随项目发布）：

```powershell
$env:BABELIAN_REFERENCE_DIR='保存三张原始PNG的目录'
npm run test:babelian-samples
```

未做浏览器操作或手机实机测试；这是本机像素算法测试，不能替代其他故事书截图的验证。

## 1.9.4：按用户要求纳入贴近外圈的字形

只修改通用边界搜索规则，没有给第2组或字母U添加特例。识别引擎仍不接收答案；答案只用于评分。最外参考路径在各方位增加基底拟合宽度2.5%的径向余量，圈内和边界带各保留每字母最多8个粗搜索起点，再用同一像素拟合与置信条件核验。单纯合用8个起点曾挤掉正确T的候选，故未采用。余量之外的定位符仍不加入主结果和备选，界面与Worker共享规则。

同一2531×219原图实测：第二组从 `NTHO?` 改为 **NTHOU**。U的拟合定位符超出最终严格参考线约2.29像素，小于4.58像素余量；像素覆盖评分约0.9707，整组重建分离度约0.2970（均不是正确率）。原N/T/H/O的拟合位置不变。

| 指标 | 1.9.3 | 1.9.4 |
| --- | --- | --- |
| 检出大螺旋 | 16 | 16 |
| 检出小螺旋 | 77 | 77 |
| 输出确定字母的项目 | 74 | 75 |
| 完整读序正确的组 | 10/16 | 11/16 |
| 字符编辑距离 | 9 | 8 |

完整读序：

```text
ELEVE NTHOU SANDF IVEHU NDRED NINET NYINE MINUS SEVEN THOU AD?I XHUND REDEI GHTYQ L?SI XTEEN
```

其余15组输出没有变化，第7组错序、第10/11/15组缺项/待确认、第14组P→Q仍存在。整图实测127.2秒；旧四组白底图28.5秒，仍为 `FFT / YFOR / TYE / IGHT`，14/14有效字母正确，不补第一组错误朝向的I。所有输出定位符和备选都通过新边界检查；几何回归验证在线、近界、超界、开放端、不同旋转和缩放。不能据此保证其他图片不会误收邻组。

旧BDAEFC真实裁片单独拟合时两极性均输出BDAE：近界E纳入，较远F/C仍不返回；E覆盖评分约0.9981、重建分离度约0.3666，不代表所有截图正确率。把同图复制两份后，经整图自动分组仍输出BDA/BDA，均漏E，保留旧版基线；自动裁片和搜索网格不同，边界允许并不保证每次都能找到。现有存档与手动项不清除，重新识别才应用新规则。巴别语、生成朝向及首交点读序规则不变。下面保留旧版本验证结果，旧“严格圈外忽略”描述仅代表当时规则。

## 1.9.3：16组反色长图（80字）

使用用户提供的2531×219原始PNG，没有手工切成16框，也没有将答案输入识别器。期望串仅用于测试评分。此图参与了算法开发，不是独立盲测。

| 指标 | 1.9.2 | 1.9.3 |
| --- | --- | --- |
| 检出大螺旋 | 0/16 | 16/16 |
| 检出小螺旋 | 0 | 77 |
| 输出确定字母的项目 | 0 | 74（其中仍有错误） |
| 完整读序正确的组 | 0/16 | 10/16 |
| 整段与答案的字符编辑距离 | 80 | 9 |

“确定项”不是正确项。编辑距离按恢复答案所需的最少单字符插入、删除、替换次数计算；`?`也算待改字符，错序会贡献多次操作。**不能将77或74当作正确识别数，更不能称为80/80。** 该图最后一次本机完整运行112.4秒，速度取决于设备；更精细的几何搜索明显慢于旧版。上一张四组白底图重新验证仍为 `FFT / YFOR / TYE / IGHT`，保留14/14有效字母；错误朝向的I没有强行补入。

| 组 | 用户答案 | 自动读序 |
| --- | --- | --- |
| 1 | ELEVE | ELEVE |
| 2 | NTHOU | NTHO? |
| 3 | SANDF | SANDF |
| 4 | IVEHU | IVEHU |
| 5 | NDRED | NDRED |
| 6 | NINET | NINET |
| 7 | YNINE | NYINE |
| 8 | MINUS | MINUS |
| 9 | SEVEN | SEVEN |
| 10 | THOUS | THOU |
| 11 | ANDSI | AD?I |
| 12 | XHUND | XHUND |
| 13 | REDEI | REDEI |
| 14 | GHTYP | GHTYQ |
| 15 | LUSSI | L?SI |
| 16 | XTEEN | XTEEN |

表中的 `?` 对应网页的 `[?]`，不是由词义补出的字母。剩余问题：

- 第2组U的高支持拟合定位符超出当时最外参考线约3.53原图像素；将基底中心及角度各扰动±1像素/度的27种组合中仍在外。遵循用户已有的圈外忽略规则，不将其强行加入，保留内部近似字形的问号。
- 第7组字形已找到，但最早交点对亚像素位置敏感：0.5像素的字形尺寸扰动即可改变前两字顺序，两种读序对应的像素重建收益只差约0.034%。不使用正确答案反向选择几何位置；需要人工复核顺序。
- 第10、11、15组仍有漏项，第14组仍把P识别成Q。尝试扩大尺寸范围和简单改成插值支持评分未得到稳健改善，未采用；不能把所有漏项都归因于圈外规则。

已采用的改进：长条图重叠分窗与全局去重；用边缘背景估计极性，避免纹理图的单一亮色笔画被误认为背景；低对比软阈值；截图朝向搜索扩大到±30°；候选与基底的联合像素精调。最终候选按最终基底重新验证边界和覆盖分数，置信阈值没有降低。生成器规则、巴别语映射及本机处理原则不变。

新增合成回归覆盖12组连通横条、窗口相位改变、竖条，以及6组低对比纹理和反色。原先合成ABCDEF受初始基底拟合偏移影响只保留ABDE；现在精调后的最终参考线合法包含C，得到ABCDE，因此该测试预期作了有依据的更新，边界条件本身不变。未执行浏览器或安卓实机操作。

可选择运行私有图片测试（图片不打包，不自动下载）：

```powershell
$env:SIREN_INVERSE_FIXTURE='用户保存的16组反色原图.png'
npm run test:siren-inverse
```

## 1.9.2历史对照

本次使用本机模板与像素计算，不调用在线识别、不用英文含义反推字母。保留网页与现有操作方式，不需要安装识别服务。以下是开发样例结果，不是陌生图片的准确率承诺。

## 塞壬语：用户提供的四组真实截图

标注为 `FIFT / YFOR / TYE / IGHT`。用户指出第一组的I朝向有误，要求排除该项。图片不随源码或接入包发布。

| 项目 | 1.9.1基线 | 1.9.2 |
| --- | --- | --- |
| 自动检测的大螺旋 | 4组 | 4组 |
| 明确输出的字母串（?表示候选待确认） | `?T? / Y??? / ?Y? / ????` | `FFT / YFOR / TYE / IGHT` |
| 有效字母明确且正确 | 3项 | 14/14 |

第一组I没有强行补回，工具输出不是完整 `FIFT`。14/14只针对排除该项后的本图；此图参与算法调试，不能视为独立盲测。一次实际Worker链路运行约9.5秒；仅为开发机测量，不是手机/浏览器耗时保证。

定位后保留更多候选，将候选与基底及其他字母合成，再用原图笔画误差作整组核验。核验保留原图局部细节，最长边上限900像素；候选定位阶段没有升级为全分辨率重新拟合。圈外竞争形状可作为“不够确定”的证据，但不会加入当前组或出现在可选结果中。

额外素材合成检查覆盖A–Z及重复字的六组布局，按最外参考线筛选后24个有效位置：基线首选23/24、明确正确18/24；新版首选及明确正确均为24/24。这些样例与模板同源，也不能代替独立实拍验证。原有空白、纯基底、反色、双组、圈外竞争项与未知项回归保留。

## 巴别语：1.9.2时同一套58字形的合成截图

默认精细比对结合32×32与64×64特征。对照关闭此选项的旧评分，映射与置信门槛不变。

| 字形高度 | 旧首选正确 | 新首选正确 | 旧/新明确正确 |
| --- | --- | --- | --- |
| 64像素 | 58/58 | 58/58 | 57/57 |
| 40像素 | 58/58 | 58/58 | 55/56 |
| 28像素 | 55/58 | 57/58 | 0/0 |
| 合计 | 171/174 | 173/174 | 112/113 |

28像素低清图仍全部要求复核：首选更准确不意味着可以直接确认。新增检查要求被自动确认的结果与测试字形相符。

额外16种乱序/模糊条件中，并非每组都改善：20像素模糊条件首选44→43，40像素模糊条件55→54；没有新增错误却被自动确认的结果。24像素模糊条件旧、新均有分割失败。因此提供 **多尺度精细比对** 开关，模糊图可关闭重试。该轮当时没有人工逐字标注的真实截图，不能据173/174推算故事书准确率；后来补入的1.9.5三张真实截图测试见本记录开头。

## 复现与边界

```powershell
npm run build
npm test
$env:SIREN_BENCHMARK_FIXTURE='用户保存的四组示例.png'
npm run test:siren-sample
```

未设置示例路径时，样例测试明确跳过；不会下载或读取其他个人文件。期望串仅存在测试代码中，不进入识别引擎。`npm test`覆盖离线图像算法、DOM/Canvas替身、存档与接入包一致性；没有运行浏览器自动操作或安卓实机测试。

原图仍只在本机处理，映射配置不变。已打开页面需先导出塞壬语工作并复制未保存文本，再刷新加载新版本。旧工作可载入后“重新匹配本组”，但原来已缩小的裁片无法恢复细节；用原始截图重新识别更适合比较此次改进。
