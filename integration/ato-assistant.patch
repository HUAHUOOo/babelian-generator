--- a/assets/page-focus-router.js
+++ b/assets/page-focus-router.js
@@ -23,6 +23,7 @@
   function moduleFromUrl(value = window.location.href) {
     const url = new URL(value, window.location.href);
     const path = url.pathname.replace(/\/+$/, "").toLowerCase();
+    if (path.endsWith("/babelian/index.html") || path.endsWith("/babelian")) return "babelian";
     if (path.endsWith("/map/index.html") || path.endsWith("/map")) return "map";
     if (path.endsWith("/record/index.html") || path.endsWith("/record")) return "record";
     if (path.endsWith("/hero/index.html") || path.endsWith("/hero")) return "hero";
--- a/api/campaign-state.php
+++ b/api/campaign-state.php
@@ -27,7 +27,7 @@
 header('Content-Type: application/json; charset=utf-8');
 header('Cache-Control: no-store');
 
-$allowedSections = ['dashboard', 'map', 'record', 'technology', 'heroes', 'aibp', 'story'];
+$allowedSections = ['dashboard', 'map', 'record', 'technology', 'heroes', 'aibp', 'story', 'babelian'];
 $dataDir = dirname(__DIR__) . DIRECTORY_SEPARATOR . 'data';
 $usersFile = $dataDir . DIRECTORY_SEPARATOR . 'ato-users.json';
 $secondScreensFile = $dataDir . DIRECTORY_SEPARATOR . 'ato-second-screens.json';
@@ -55,6 +55,7 @@
       'heroes' => null,
       'aibp' => null,
       'story' => null,
+      'babelian' => null,
     ],
     'sectionRevisions' => [
       'dashboard' => 0,
@@ -64,6 +65,7 @@
       'heroes' => 0,
       'aibp' => 0,
       'story' => 0,
+      'babelian' => 0,
     ],
   ];
 }
@@ -822,6 +824,19 @@
   }
 
   $campaign = read_campaign($saveFile);
+  // Fence the save against an account or campaign switch during the request.
+  if ($payloadSection === 'babelian') {
+    if (($payload['ownerId'] ?? '') !== $user['id']) {
+      respond(403, ['ok' => false, 'error' => 'Babelian account changed.']);
+    }
+    $activeBabelianProfile = (string) ($campaign['sections']['dashboard']['activeProfileId'] ?? 'default');
+    if (($payload['profileId'] ?? '') !== $activeBabelianProfile) {
+      respond(409, ['ok' => false, 'code' => 'BABELIAN_CONTEXT_CHANGED', 'error' => 'Babelian campaign changed.']);
+    }
+    if (!isset($payload['expectedRevision'])) {
+      respond(400, ['ok' => false, 'error' => 'Babelian revision required.']);
+    }
+  }
   $expectedRevision = $payload['expectedRevision'] ?? null;
   $currentRevision = (int) ($campaign['sectionRevisions'][$payloadSection] ?? 0);
   if ($expectedRevision !== null && (int) $expectedRevision !== $currentRevision) {
--- a/index.html
+++ b/index.html
@@ -2044,6 +2044,7 @@
         </label>
         <div class="record-links">
           <a class="record-card-link" href="./record/index.html">阿尔戈号记录表</a>
+          <a class="record-card-link" href="./babelian/index.html">巴别语解密</a>
           <a class="record-card-link hero-card-link" href="./hero/index.html">英雄记录表</a>
           <a class="record-card-link aibp-card-link" href="./aibp/index.html">AIBP</a>
           <a class="record-card-link technology-card-link" href="./technology/index.html">科技</a>
@@ -3315,7 +3316,11 @@
 
     function clearLocalStorageAfterLogout() {
       try {
+        const babelianPending = Object.keys(localStorage)
+          .filter(key => key.startsWith("ato-babelian-pending-v1:"))
+          .map(key => [key, localStorage.getItem(key)]);
         localStorage.clear();
+        for (const [key, value] of babelianPending) localStorage.setItem(key, value);
       } catch (error) {
         console.warn("无法清空本地存储：", error);
       }
@@ -7641,6 +7646,26 @@
       }
     }
 
+    async function saveImportedBabelian(state) {
+      const read = async query => {
+        const response = await fetch(`${authUrl}${query}`, { cache: "no-store" });
+        const value = await response.json();
+        if (!response.ok || !value.ok) throw new Error(value.error || "巴别语存档读取失败");
+        return value;
+      };
+      const me = await read("?action=me");
+      if (!me.authenticated) throw new Error("请先登录后再导入巴别语存档。");
+      const current = await read("?section=babelian");
+      const dashboard = await read("?section=dashboard");
+      const response = await fetch(`${authUrl}?section=babelian`, {
+        method: "POST", headers: { "Content-Type": "application/json" },
+        body: JSON.stringify({ section: "babelian", state, ownerId: me.user.id,
+          profileId: String(dashboard.state?.activeProfileId || "default"), expectedRevision: current.revision }),
+      });
+      const result = await response.json();
+      if (!response.ok || !result.ok) throw new Error(result.error || "巴别语存档未导入，请单独重试。");
+    }
+
     function downloadJsonPayload(payload) {
       const text = JSON.stringify(payload, null, 2);
       const blob = new Blob([text], { type: "application/json;charset=utf-8" });
@@ -7669,6 +7694,7 @@
           record: serverSections.record || null,
           technology: serverSections.technology || null,
           heroes: serverSections.heroes || null,
+          babelian: serverSections.babelian || null,
         };
         const payload = {
           app: "ATO Campaign Save Package",
@@ -7726,6 +7752,7 @@
             sections?.technology ? saveImportedCampaignSection("technology", sections.technology) : Promise.resolve(),
             sections?.heroes ? saveImportedCampaignSection("heroes", sections.heroes) : Promise.resolve(),
           ]);
+          if (sections?.babelian) await saveImportedBabelian(sections.babelian);
           campaignSectionRevision = Math.max(0, Number(dashboardSave?.revision || campaignSectionRevision));
           campaignSectionExists = Boolean(dashboardSave?.ok || campaignSectionExists);
           campaignSectionBaseline = cloneJson(archive);
--- a/story/index.html
+++ b/story/index.html
@@ -16,6 +16,7 @@
         </div>
       </div>
       <a class="home-link" href="../index.html">返回主控台</a>
+      <a id="babelianToolLink" class="home-link" href="../babelian/index.html">巴别语解密</a>
       <a class="home-link" href="../record/index.html" style="margin-top:-12px;margin-bottom:4px;font-size:13px;border-color:#c89742;color:#ffd480;background:#17140f;">阿尔戈号记录表</a>
       <a class="home-link" href="../hero/index.html" style="margin-top:0;margin-bottom:18px;font-size:13px;border-color:#c89742;color:#ffd480;background:#17140f;">英雄记录表</a>
 
--- a/story/assets/app.js
+++ b/story/assets/app.js
@@ -2332,6 +2332,8 @@
     if (entry.encounterKey) params.set("encounter", entry.encounterKey);
     params.set("entry", entry.id);
     window.history.replaceState(null, "", `${window.location.pathname}?${params.toString()}`);
+    const babelianLink = document.getElementById("babelianToolLink");
+    if (babelianLink) babelianLink.href = `../babelian/index.html?${params.toString()}`;
   }
 
   function showEntry(entry, pushHistory) {
--- a/tools/packaging/android/app/src/main/java/com/ato/assistant/LocalCampaignApi.java
+++ b/tools/packaging/android/app/src/main/java/com/ato/assistant/LocalCampaignApi.java
@@ -11,7 +11,7 @@
 import java.util.UUID;
 
 final class LocalCampaignApi {
-  private static final String[] SECTIONS = {"dashboard", "map", "record", "technology", "heroes", "aibp", "story"};
+  private static final String[] SECTIONS = {"dashboard", "map", "record", "technology", "heroes", "aibp", "story", "babelian"};
   private static final int BACKUP_COUNT = 10;
   private final SharedPreferences store;
   private final Object lock = new Object();
@@ -284,6 +284,15 @@
 
     JSONObject campaign = loadCampaign();
     JSONObject revisions = campaign.getJSONObject("sectionRevisions");
+    if ("babelian".equals(section)) {
+      if (!user().optString("id").equals(payload.optString("ownerId")))
+        throw new ApiException(403, error("Babelian account changed."));
+      JSONObject dashboard = campaign.getJSONObject("sections").optJSONObject("dashboard");
+      String active = dashboard == null ? "default" : dashboard.optString("activeProfileId", "default");
+      if (!active.equals(payload.optString("profileId")))
+        throw new ApiException(409, error("Babelian campaign changed."));
+      if (!payload.has("expectedRevision")) throw new ApiException(400, error("Babelian revision required."));
+    }
     int revision = revisions.optInt(section, 0);
     if (payload.has("expectedRevision") && payload.optInt("expectedRevision", -1) != revision) {
       JSONObject conflict = error("This section was changed in another page.");
--- a/tools/packaging/android/app/src/main/java/com/ato/assistant/MainActivity.java
+++ b/tools/packaging/android/app/src/main/java/com/ato/assistant/MainActivity.java
@@ -20,6 +20,32 @@
   private static final String ROOT = "file:///android_asset/web/";
   private static final int FILE_CHOOSER_REQUEST = 1001;
   private static final int ATOPACK_CHOOSER_REQUEST = 1002;
+  private static final int BABELIAN_SAVE_REQUEST = 1003;
+  private String babelianSaveId;
+  private byte[] babelianSaveBytes;
+
+  private void notifyBabelianSave(String id, boolean ok, boolean cancelled, String message) {
+    org.json.JSONObject result = new org.json.JSONObject();
+    try {
+      result.put("id", id); result.put("ok", ok); result.put("cancelled", cancelled); result.put("error", message);
+    } catch (org.json.JSONException ignored) {}
+    runOnUiThread(() -> webView.evaluateJavascript(
+      "window.BabelianNativeFileResult && window.BabelianNativeFileResult(" + result.toString() + ")", null));
+  }
+
+  private void finishBabelianSave(int resultCode, Intent data) {
+    String id = babelianSaveId; byte[] bytes = babelianSaveBytes;
+    babelianSaveId = null; babelianSaveBytes = null;
+    if (id == null) return;
+    if (resultCode != RESULT_OK || data == null || data.getData() == null) {
+      notifyBabelianSave(id, false, true, ""); return;
+    }
+    try (java.io.OutputStream output = getContentResolver().openOutputStream(data.getData(), "wt")) {
+      if (output == null || bytes == null) throw new java.io.IOException("无法创建文件。");
+      output.write(bytes); output.flush(); notifyBabelianSave(id, true, false, "");
+    } catch (Exception error) { notifyBabelianSave(id, false, false, String.valueOf(error.getMessage())); }
+  }
+
   private WebView webView;
   private ValueCallback<Uri[]> filePathCallback;
   private LocalCampaignApi localApi;
@@ -83,6 +109,7 @@
 
   @Override protected void onActivityResult(int requestCode, int resultCode, Intent data) {
     super.onActivityResult(requestCode, resultCode, data);
+    if (requestCode == BABELIAN_SAVE_REQUEST) { finishBabelianSave(resultCode, data); return; }
     if (requestCode == ATOPACK_CHOOSER_REQUEST) {
       if (resultCode != RESULT_OK || data == null || data.getData() == null) {
         notifyAtopackResult(errorResult("已取消导入"));
@@ -104,6 +131,36 @@
   }
 
   private final class LocalApiBridge {
+    @android.webkit.JavascriptInterface public void saveBabelianFile(String id, String filename, String mime, String base64) {
+      if (id == null || id.length() > 120) return;
+      if (base64 == null || base64.length() > 12 * 1024 * 1024 ||
+          !("image/png".equals(mime) || "application/json".equals(mime))) {
+        notifyBabelianSave(id, false, false, "文件类型或大小不支持。"); return;
+      }
+      final byte[] bytes;
+      try {
+        bytes = android.util.Base64.decode(base64, android.util.Base64.DEFAULT);
+        if (bytes.length > 8 * 1024 * 1024) throw new IllegalArgumentException();
+      } catch (Exception error) { notifyBabelianSave(id, false, false, "文件数据无效或超过8MB。"); return; }
+      runOnUiThread(() -> {
+        if (babelianSaveId != null) { notifyBabelianSave(id, false, false, "请先完成上一次保存。"); return; }
+        String safeName = filename == null ? "babelian" : filename.replaceAll("[\\\\/:*?\"<>|\\p{Cntrl}]", "_");
+        if (safeName.length() > 120) safeName = safeName.substring(0, 120);
+        String extension = "image/png".equals(mime) ? ".png" : ".json";
+        if (!safeName.toLowerCase(java.util.Locale.ROOT).endsWith(extension)) safeName += extension;
+        babelianSaveId = id; babelianSaveBytes = bytes;
+        try {
+          Intent intent = new Intent(Intent.ACTION_CREATE_DOCUMENT);
+          intent.addCategory(Intent.CATEGORY_OPENABLE); intent.setType(mime);
+          intent.putExtra(Intent.EXTRA_TITLE, safeName);
+          startActivityForResult(intent, BABELIAN_SAVE_REQUEST);
+        } catch (Exception error) {
+          babelianSaveId = null; babelianSaveBytes = null;
+          notifyBabelianSave(id, false, false, String.valueOf(error.getMessage()));
+        }
+      });
+    }
+
     @android.webkit.JavascriptInterface public String request(String url, String method, String body) {
       return localApi.handleForJavascript(Uri.parse(url), method, body == null ? "" : body);
     }
