# 塞壬语生成与翻译指南

默认配置采用所提供并校准的26字母字形表，生成与识别共用同一组素材。键盘上排 A→M、下排 Z→N，同列是圈数相同但方向相反的字母；无需先填写映射。

1.9.7新增两行配对键盘（A→M / Z→N）、疑难区域深度识别及显式校正样本库。窄屏键盘横滑，不重新换成多行，以保持A/Z、B/Y等上下对齐。

深度识别只在已识别的大螺旋上使用：选中项后默认框住其中心附近，也可拖框重新限定中心范围；它不会建立新的大螺旋截图。固定已校准的大螺旋，使用原始裁片进行更密集的位置/尺寸/朝向搜索及像素核验。框外笔画保留；朝向仍限制在默认方位±30°，大小仍为大螺旋宽度的0.33–0.44，外圈余量仍为2.5%。候选均为待复核，不自动采用。点击候选后在原图叠加预览；确认采用才会替换所选项或新增，可撤销。位置锁定时不能替换。

校正样本库与塞壬语工作JSON分开。核实后收录当前字形、原图裁片、基底及字形位置/大小/朝向；可改标注、备注、排除训练或删除，导入导出不改变画布。样本暂不参与自动识别，也不自动训练；离开前务必单独导出。QQ20260911中第一组V的错误朝向按用户说明排除有效字母评估，不放宽规则补入V。

1.9.6改进高反差但细线发灰、边缘模糊的截图：按前景色阶判断是否使用软阈值，保留清晰图及原低对比处理方式。字母确认门槛、边界及生成规则不变；不是按英文补字。新示例仍有漏项，详见[识别验证记录](integration/RECOGNITION_ACCURACY.md)。巴别语识别沿用1.9.5。

打开 `index.html`，首页选择 **塞壬语生成** 或 **塞壬语翻译**，直接进入对应模式。“返回首页”只切换界面，不清空文字、螺旋、截图或撤销历史。生成和翻译各自保留工作；巴别语备份按钮仅在巴别语功能内显示，塞壬语仍须使用自己的“保存塞壬语工作”。

## 生成螺旋

1. 初始已有一个空的大螺旋。字形键盘默认收起，点击 **新增其他小螺旋** 才展开并进入“点击添加”，连续点击会添加多个字形；点击已有小螺旋或列表项只显示候选与操作按钮，不自动展开键盘；点击 **选择其他小螺旋** 后才从完整键盘替换，选完收起。点击画布空白处取消选择、恢复添加，下一字形放在所点击的位置；添加后继续保持添加。每组最多六个，缩略图下方第二行的 **＋ 大螺旋** 新建下一组。**批量生成** 按钮展开英文框，确认后收起，按六字母一组排布。输入仅接受英文字母和空白，统一大写，空白不编码；最多50组、300字母。普通添加或手动拖动后按实际交点显示语序，批量生成会搜索符合输入顺序的位置。
2. 小螺旋可在画布内自由拖动，不限制固定半径或环形轨道。**自动模式**的朝向由相对大螺旋中心的位置决定：正下方0°，正上方180°，正右方−90°，正左方90°；恰在中心时采用0°。在精确调节中点击“自动”切为“可调”，先保留当前显示角度，再用数值或滑块修改；此后移动小螺旋、调整位置或大螺旋中心不改变该角度。切回自动恢复按位置计算。拖动期间保留上次语序并暂时禁用复制/分词/翻译，松手后统一更新。
3. **语序按每个小螺旋与大螺旋的最先交点排序，不按小螺旋中心距离。** 沿大螺旋路径从中心向外检查所有相交位置，一个小螺旋跨过多圈时取最先碰到它的位置。红圈标出首交点，选中项的其他交点显示为金圈，可打开大螺旋参考路径检查。不同组连续拼接，不是完整单词边界。首交点相差不足1个画布路径单位时，相关项显示 `[?]`；若某字形根本没有交点，整组暂用 `[?]` 保留，不能假设它在已定位字形之前或之后。
4. 编辑画布最大宽度760px，窄屏自动适应。小螺旋选择按钮与 **精确调节** 在画布下方横排，窄屏可滚动选择项。精确调节默认收起；位置半径、环绕角度、字形朝向、大螺旋大小均有联动数值框与滑块。朝向仅在“可调”模式下可编辑。半径滑块按画布边界限制范围，环绕滑块必要时缩短半径以留在画布内；连续滑动一次只记录一步撤销。大螺旋大小同步缩放小螺旋，不提供大螺旋旋转或小螺旋单独大小控件。截图模式保留有边框的“删除所选小螺旋”按钮；生成模式没有该按钮。组仍可前后移动、删除，支持30步撤销。画布有焦点时也能输入英文字母。
5. 原尺寸缩略图（图像80×80 CSS像素）从左到右连接为第一行，兼作预览与编辑组选择；窄屏横向滚动，新组追加到右端，不再另设放大的整幅预览。**＋ 大螺旋 / 批量生成 / 导出整幅 PNG** 位于第二行。导出一次包含所有大螺旋，去掉序号、中心标记和英文字母标签，不再每三组换行。各组先在600×600透明画布绘制并裁掉画布外笔画，再按真实墨迹裁左右留白，共用垂直范围、保留6个逻辑像素间距和外边距。超长时整行等比缩小（默认最长边8192、最多800万像素）；若分配失败再降低分辨率，不拆行，并显示实际导出尺寸。

### 几何模型的边界

当前模型依据用户更正后的规则：每组六小螺旋、沿大螺旋首交点决定语序、自由拖动。自动朝向为 `normalize(atan2(y−cy, x−cx) × 180/π − 90)`，画布Y轴向下，不累积拖动角度或叠加大螺旋旋转。手动模式使用固定设置角度，并用该角度同时绘制字形、计算交点及原图覆盖度。−180°与180°等价。旋转**不会镜像字形或改字母**。“位置半径”仅用于数值定位，不参与语序计算。

26个字形保留同一420×420原图裁剪框，自转中心取该框中心；大螺旋内眼位置使用近似枢轴。IMG_3491中六个字形的拟合画布宽比为0.3768–0.3804，统一取 **小图画布宽=大图画布宽×0.38**：大螺旋500时小螺旋190，较旧默认110放大约73%；示例未出现的其余20字母同样处理。不提供单独大小调节入口；在“精确调节”中修改大螺旋大小会按比例同步缩放已有小螺旋，内部30–400范围不变，超过范围取边界值。旧工作中已保存的自定义大小和大螺旋旋转参数仍保留，避免擅自改变已有布局。不把近似枢轴和生成布局宣称为与官方逐像素一致，亦不重绘原始笔触。

主路径从原始700×700大螺旋素材提取有序中心线，排除两个离散墨点，只连接一处9.22原图像素的细断裂，提取记录保存在 `src/siren-path.json`。相交计算将原始小字形墨迹按画布的实际位置、旋转与大小反变换采样，沿主路径以不超过0.5画布像素的步长遍历（alpha阈值128），合并同一接触区内的小间隙。因此交点是栅格笔画的近似接触位置，细擦边及源图断裂仍可能需要微调；不是小螺旋中心或外接矩形碰撞。

批量生成也采用相同的0.38尺寸比例与位置朝向规则，按交点顺序寻找合适布局，保持输入字母不变；不会在布局完成后偷偷调换字母以凑答案。用户自由移动、缩放或改变大螺旋后，语序可以随真实交点改变。

### IMG_3491 校准样例

从用户新图独立拟合B、D、A、E、F、C的模板位置，原始独立拟合的字形前景覆盖约97–99%。1.5.2保留这些位置，统一大小为0.38比例并按最新规则重算朝向后，同一交点引擎仍得到 **BDAEFC**；不声称新固定参数仍具有相同像素覆盖率。旧中心距离法得到 ADBFEC。原始拟合参数在 `tests/fixtures/siren-3491.json`，最新规则的回归样例由 `tests/siren-fixture.cjs` 构造，不在生成器中硬编码答案。本图的交点半径排序与沿路径首次相交排序恰好一致，因此它能否定小螺旋中心排序，但不能单凭此图区分两种交点排序法；工具按“沿大螺旋从内向外”的规则实现。

## 从截图识别与复核

1. 切换 **截图 → 识别与复核**，选择、拖入或直接 **Ctrl+V** 粘贴 PNG / JPG / WebP 图片（最多16MB、2400万像素、单边12000像素）。也可点击“粘贴图片”；浏览器不支持按钮读取或未允许读取时，用 Ctrl+V 或文件选择。只在塞壬语截图工作区响应图片粘贴，不截获文字或巴别语工作区的粘贴。
2. 点击 **自动分割并匹配**，直接识别整张图片。预览不再提供拖框、X/Y/宽高或“将选区加入解读”。自动搜索大螺旋的位置、尺寸及旋转，再比较26个小字形的位置、大小和朝向。整图保留至最长边3600像素且不超过1200万像素；尽量选用只包含螺旋的截图，细小字形可在外部裁成较小图片再载入。每次最多20组，每份工作最多50组。
3. 识别结果按行从左到右追加为解读组，不覆盖已有组。几何与编辑坐标仍为600×600逻辑单位；原图裁片按来源清晰度保存为600–1200像素正方形，超出单张存档体积限制时降采样，旧600像素裁片仍可重配。长条图使用重叠窗口搜索和跨窗去重，不按已知组数切图；根据边缘背景判断反色，低对比图自动做软阈值处理。候选定位后，在原图局部最高900像素下进行 **整组像素核验与精调**：比较“大螺旋＋其他字母＋当前候选”的笔画叠加效果，小步调整候选及基底位置、大小、角度，并用最终基底重算边界及覆盖评分。低清原图不会凭空增加细节；高分字母的交点语序仍可能不稳定。不调用词典、语言模型或在线OCR，不假定每组必须六项。较长图片需要更久，可随时取消。
4. 点击小螺旋或画布下方的选择按钮，查看最多三个候选及笔画匹配分数。每个候选的字母与分数上方显示原素材字形，按该候选的拟合角度旋转，与采用后叠加到截图上的朝向一致；不会跟随键盘的临时对照点改变。候选按整组核验排序，因此其显示分数不一定降序：显示值是单个模板的笔画覆盖评分，**不是正确率**，也不是整组核验的排序分值。证据不足或竞争接近时仍用 `[?]` 保留。点击候选会采用该候选自己的位置、大小和拟合角度，重新计算首交点。也能从完整26键手动指定字母；手动改字或移动后，旧匹配记录失效，必要时重新标记首交点。
5. **点击画布的任意位置，26个键盘字形同时转向该点对应的方位。** 正下方0°、正上方180°、正右方−90°、正左方90°。选择列表项或拖动时跟随该项位置。只改变键盘的对照朝向，不旋转截图、不修改字母。截图拟合允许在方位规则附近微调±30°以适应实际图片，叠加模板与精确调节的朝向字段显示拟合角度；键盘和生成器仍按原标准角度显示。
6. 在“精确调节”中开启“叠加所选模板”和参考路径，比较匹配位置。匹配字形按真实笔画与大螺旋主路径的首交点排序，不按小螺旋中心半径排序。未知项暂用第一候选的形状定位，但字母仍为 `[?]`；其语序也需要复核。截图模式保留其余已定位字母：例如 `AB[?]D EF`。若未知项没有有效交点，则显示 `ABD EF`，并在状态栏单独列出未定位项，不猜测缺项位置；全部无法定位的组以 `[?]` 保留。空格仅分隔大螺旋，并非英文词界。生成模式仍保留无交点时的整组警示行为。
7. **重新匹配本组** 会对当前裁片重新搜索大小螺旋；已有项存在时先确认，完成后可撤销。**取消识别** 不改变旧工作；切换语言/模式、载入新图或存档会取消旧任务。识别期间若编辑了工作，旧结果不会覆盖这些编辑。后台识别要求浏览器支持本地 Blob Worker；不支持时需更换浏览器，仍可编辑已载入的工作。
8. 邻组小螺旋误归入当前组时，选中该项，点击画布下方 **删除所选小螺旋**。只删除识别标记，不擦除原始截图，也不自动改动邻组；可用 **撤销** 恢复。该按钮仅在截图模式出现。满六项时可先删除误识别项，再添加漏掉的字母。

### 自动识别的最外圈限制

沿“大螺旋中心→定位符中心”的射线与参考路径求交，取最外侧交点为该方位的边界。1.9.4按用户要求，在此半径上增加**大螺旋拟合宽度2.5%**的余量；不是半径的2.5%，也不是固定像素。小螺旋定位符在边界及余量内才可参与自动识别，超出仍直接忽略。判断跟随大螺旋对齐的大小、位置及旋转，使用原素材枢轴，不用固定圆、外接矩形或最近组距离，也不把开放螺旋首尾补成闭合线。

粗搜索对圈内和新增边界带分别保留每字母最多8个起点，防止边界候选挤掉原有圈内起点；之后精细匹配、整组像素核验和界面备选均按同一余量过滤。每组六项上限、像素覆盖及置信阈值不变，不因属于边界带就直接确认字母。反色长图第二组U的最终定位符在严格线外约2.29原图像素、余量约4.58像素，现可正常纳入。此规则不指定任何字母或组号，也不用英文语义。只限制新识别及“重新匹配本组”的结果，不清除旧存档、手动新增或拖动项，不自动转移到邻组。已有BDAEFC仍完整保留；自动识别是否完整仍取决于图像证据。

### 锁定与复位

选中项后，候选区右侧提供 **锁定位置 / 解锁位置**。锁定期间不能拖动、用数值/滑块修改几何或采用其他候选；有锁定项时不能改变该组基底大小与中心。先解锁再修改。删除、撤销和经过确认的整组重新匹配仍可使用，重新匹配会替换锁定项。点画布仅获得键盘焦点，不自动滚动或展开字形键盘。

识别项移动后失去有效匹配，选中其 **未定位** 按钮可见 **复位**。复位恢复最近一次自动识别或采用候选的位置、大小、朝向和匹配记录，可撤销；不改变原图或邻项。手动改过字母时保留该字母，清除旧字母的匹配分数，按当前字形模板重算交点。复位记录连同基底参数保存于JSON，载入后仍可使用；基底校准已变时禁用单项复位，需重新匹配本组，避免影响其他项。旧版尚有拟合数据的项在载入时建立基准；已经丢失拟合记录的旧未定位项无法凭空恢复，需重新匹配。

“新增其他小螺旋”位于删除按钮旁，“选择其他小螺旋”位于候选旁。只有这两个按钮展开完整键盘；普通点选、切换组、载入工作和切换模式均不自动展开。新增模式可连续添加，也可“收起”；选择其他字形后自动收起。界面保留简短错误/未定位提示，移除重复读序与长段说明。

### 手动校正

自动结果不完整时，在已识别的组中补充或校正。可标记大螺旋内眼中心，在精确调节中调整大螺旋大小，使参考路径对齐；也可直接“重新匹配本组”。旧工作中的手动裁片仍可载入编辑，但不再从预览手动建立裁片组。

点小螺旋中心的空白位置，再用字形键添加字母。新增项始终以**紫色半透明字形**叠加，并标注“人工”，方便和原图比较；关闭普通模板叠加也不会隐藏它。附近已有项难以避开时，可先在别处添加再拖动，或使用精确位置数值。松手后计算固定位置、大小和朝向下的原图覆盖度；另显示排除当前大螺旋笔画后的覆盖度，独立笔画不足时提示证据不足。评分只读取未叠加的原始裁片，不会把紫色字形自己当作匹配证据，不会自动搜索新位置或替换字母。**覆盖度不是字母正确率或交并比；邻组笔画仍可能抬高分数。**

手动新增项按模板与大螺旋的首交点自动排序；也可选中该项，展开精确调节，点击“标记最内侧交点”并在参考路径上点出最先相交处。离参考路径超过12画布像素的点会被拒绝，人工交点优先于自动交点。移动字形、改动大螺旋中心或大小后，相关拟合记录失效：人工新增项重新计算模板交点，旧识别项须重新匹配或人工标记。

**限制：** 自动匹配仍可能漏检或误检。低分辨率、透视变形、明显拉伸、笔画残缺、多个组紧密叠压和夹杂文字/插画会影响结果；尽量使用清晰、正视、只含螺旋的截图。多个大螺旋相连时不保证完全分开。生成器的固定几何模型与原图拟合是两件事，不能把通过单一样本验证视为所有故事书截图都准确。

## 英文整理与中文翻译

“逐字英文”只来自所选字母和几何顺序。点击“生成分词断句建议”才调用原有离线分词器；建议不作为字形匹配依据，专名和标点可能不准确。可手动修改空格和标点，不可改动字母或 `[?]`。如果底层字母/语序随后改变，已编辑文本保留但标为过期，需明确重新生成后才能复制或翻译。

截图逐字结果的组间空格在分词前移除，不把大螺旋边界当作单词边界。有未定位项时，仍可生成、编辑和复制分词建议，也可按原有同意流程翻译；状态栏提示可能缺字，未定位项单独列出，不猜测其位置或补字。

塞壬语整理层把已分词的英文基数转换为数字：`fifty forty eight → 5048`、`five hundred → 500`、`one hundred and five → 105`、`nine three zero one → 9301`、`zero five → 05`。先按合法基数短语组合，再连接连续的数字组，不将整串数词直接相加；支持零至十九、整十、hundred/thousand/million/billion。逗号、未知标记和非数字词阻断连接，`five and six → 5 and 6`，不做算术求值。普通词不按子串替换。转换后的数字不可擅改（可调整空格/标点），错误应回到字形复核；原始逐字英文与字母项完全保留，巴别语分词规则不变。

中文机翻默认折叠。展开、勾选同意并点击“翻译当前英文”后，才把此刻整理框中的英文发送到 MyMemory。**采用用户手动调整后的文本**，不发送截图、字形图、配置或战役数据。英文变化、切换模式、撤回同意、取消或关闭翻译面板会使旧译文失效；未知标记原样保留，不要求翻译服务猜字。第三方服务的限额、质量和联网要求与巴别语机翻相同。

## 保存及 Assistant 适配

- 生成和截图解读是两份独立的页面内存工作。点击 **保存塞壬语工作** 只导出当前模式的 JSON，包含螺旋位置、对应字母、自动朝向/大小、截图拟合角度/分数/候选、大螺旋校准参数和人工首交点；不再保存联动系数。截图模式包含已选的大螺旋裁片，不包含原始整张截图。
- 整理英文、中文机翻和撤销历史不进入 JSON，请另行复制。**原有巴别语“完整工作备份”不包含塞壬语。** 浏览器刷新会清除未导出的塞壬语工作，修改后尝试离开会请求确认；浏览器是否显示提醒仍由宿主决定。
- 导入完整验证成功后才替换文件所属模式的工作，支持撤销；不接受远程图片地址、无效字母、超量小螺旋或未知版本。JSON最多8MB，裁片总量另有限制，过长任务请分段保存。新格式 **v6** 保存 `rotationMode`（auto/manual）、手动角度、`positionLocked` 和单层 `resetPose`，同时保留截图拟合和人工新增标记。可读取v1–v5，旧项默认自动；保留位置和字母，按既有迁移规则把v1/v2默认大小110升级为示例比例，保留自定义大小。v1截图工作需补标首交点；v3及之后的110不再放大。旧版工具不能读取v6，请使用新版。巴别语与战役存档格式不变。
- 两种构建共用代码。独立版嵌入27张塞壬语PNG；ATO版在 `babelian/assets/` 增加 `siren-base.png` 和 `siren-a.png` 至 `siren-z.png`。模块共91张PNG，清单含页面共92项。
- 沿用现有 Android PNG/JSON 保存桥接，8MB限制不变。没有增加服务器接口、权限或战役存档字段，也没有自动安装到 Assistant 原工程。

## 素材与验证

1.9.4增加边界余量、旋转/缩放不变性及圈外拒绝回归；同一反色图第2组已为NTHOU，完整正确组数从10增加到11，其他15组读序不变。旧四组图仍为14/14有效字母正确；详细成绩与剩余错误见 [识别验证记录](integration/RECOGNITION_ACCURACY.md)。严格几何函数继续单独测试，自动识别与界面统一使用带2.5%余量的函数。合成MNOPQR中近界O现可确认，自动得到MNOPQ，较远的R仍被排除。

1.9.3曾在不放宽最外圈规则的情况下增加12组长条、位移、竖条、低对比纹理/反色及空白图回归。基底精调使合成ABCDEF中原先因拟合偏移而被排除的C回到最终界内，从ABDE改善为ABCDE；1.9.4仍保持该输出。

1.9.2增加整组核验、候选排序信息的JSON保存/载入，以及高分辨率裁片；v6格式不变，仅允许可选的 `fit.method: "joint-pixel"`。用户提供的四组示例自动输出 **FFT / YFOR / TYE / IGHT**，按要求排除第一组朝向错误的I后，14个有效字母均正确；未把排除项补入结果。测试标注只在独立测试脚本中，识别引擎没有该图答案。详见 [识别验证记录](integration/RECOGNITION_ACCURACY.md)，成绩不代表其他截图的准确率。

1.9.1增加最外参考路径内/外/线上、开放端点、非圆边界、枢轴、旋转和缩放测试；验证自动结果与备选全部界内、界外候选不占名额，以及旧存档和人工项不被清除。界面替身测试检查裁剪控件与预览拖框事件已移除、整图传入Worker和图片粘贴保留。真实 IMG_3491 的自动识别回归按新规则期望 BDA，而非完整六项。

1.8.0测试覆盖滑块与数值联动、一次滑动一步撤销、手动角度随位置/中心变化保持不变、切回自动、v5保存/v4迁移、精确调整清除过期拟合参数，以及未定位项仍能整理。数字测试覆盖基数短语、数字串、前导零、标点/未知分隔、原文字母保持不变、普通单词不被转换和巴别语校验隔离。原有删除/撤销不改原图、人工标记保存、固定位置真实像素覆盖度测试保留。

字母A–Z从用户提供的 `IMG_3463.PNG` 至 `IMG_3488.PNG` 弹窗逐张核对，统一裁剪 `[393,933,813,1353]`，原生420×420像素；基底来自 `IMG_3490.PNG`，裁剪 `[240,620,940,1320]`，700×700像素。通过灰度到透明度转换去除底色，未重绘、镜像或依赖语义补字。反向配对 A↔Z、B↔Y、C↔X……M↔N 与本次素材一致。

`src/siren-glyphs.json` 附源文件名、源SHA-256、裁剪和成品SHA-256，不包含原始手机截图的界面或通知。素材复用仍须遵守原作品权利及目标项目素材规则。

测试覆盖BDAEFC校准参数回归、26字母统一大小、四方向绝对朝向、同位置不同拖动路径、中心退化情况、首交点而非中心/外框排序的反例、多交点取首、无交点/同交点、六字母上限、连续跨组读取、批量布局、v1/v2/v3迁移与v4导入校验、27张素材完整性；用轻量DOM/Canvas替身测试首页选择、键盘/粘贴隔离、自动添加/替换、空白取消、折叠参数、原尺寸横向缩略图/第二行操作/画布下方选择栏、移除旧操作后的人工解读和整张PNG导出、拖动期间冻结旧读序/松手重算、尺寸同步及撤销、人工交点和独立保存。整行布局测试覆盖共用基线、真实alpha边界、间距和50组缩放。它们是代码与离线图像分析测试，**未进行浏览器操作、完整故事书批量准确率、手机或 Android 实机验证**。
1.7.0历史测试在未限制定位符边界时，由 IMG_3491 全26类搜索读出 BDAEFC；1.9.1的严格边界自动回归改为BDA；1.9.4带余量的自动结果为BDAE（正色与反色相同），近界E可加入，F/C仍未返回，不宣称六项完整。另测仅大螺旋/空白负例、单字、重复字、字母组合、候选不确定项、Worker程序、取消/旧响应隔离和v4匹配数据。真实裁片保留在用户本地，通过 `SIREN_REAL_FIXTURE` 指定测试路径，不随项目发布；默认测试使用现有字形素材合成图片。素材派生样例不代表陌生截图的正确率。
